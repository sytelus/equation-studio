# Validation and known limits

Validation date: **25 September 2026**. This is an executed implementation, not a mock-up or a shader listing that was never compiled. Every figure below is generated by `tools/write_validation_report.py` from the captured test outputs.

## Executed test groups

| Group | Result | Evidence |
|---|---|---|
| JavaScript unit tests | **259 passed, 0 failed** | [Captured output](NODE_TEST_RESULTS.txt) |
| Retained Python source tests | **42 ran, all passed** | [Captured output](CPU_TEST_RESULTS.txt) |
| Actual WebGL scene rendering | **12/12 presets** at 640 × 384, with finite-output checks | [GPU report](GPU_VALIDATION.json) |
| Actual component shaders | **43/43 component kinds** compiled and rendered with finite default outputs | [GPU report](GPU_VALIDATION.json) |
| Per-node preview atlas | **12/12 presets** rendered a finite thumbnail for every node from one shared program | [GPU report](GPU_VALIDATION.json) |
| Bypass and contribution | A disabled modifier is an exact identity, a disabled combiner passes its input, disabled content is zero; the contribution view marks only pixels a node changes | [GPU report](GPU_VALIDATION.json) |
| One program per graph structure | Bypass flags, the output choice and every canvas view share one compiled program: **passed**; whole-graph and subgraph programs agree for every stage, largest byte difference **0** | [GPU report](GPU_VALIDATION.json) |
| Probes | Line probes match point probes and raw thumbnails match raw snapshots: **passed** | [GPU report](GPU_VALIDATION.json) |
| Components as equations (✎ Edit) | **29 components** written as equations reproduce the raw values of the built-in ones, largest difference **0** | [GPU report](GPU_VALIDATION.json) |
| Stateless animation | **9/9 tested moving presets** changed at t=2.3 and repeated the original t=0 frame exactly on this backend | [GPU report](GPU_VALIDATION.json) |
| Original source raw fields | **32 native-grid points**, seven field groups, actual float framebuffer readback vs float64 CPU | [GPU report](GPU_VALIDATION.json) |
| Original source native image | **2000 × 1200** rendered and compared without alignment or color fitting | [GPU PNG](../gallery/bipolar_2000x1200_gpu.png) |
| Editor workflows | 35 checks: Defaults: live pipeline previews for all 9 source components, rulers and grid on; A click selects without changing the canvas; This step follows the selection; [ ] and ◀ ▶ step; the canvas label names the view; View switch: effect styles, lock keeps a stage while selecting elsewhere; Rich tooltips name the control, its shortcut and its on/off state; UI key creation, tracked control editing and interpolation; Undo/redo preserves the animation model, selection and playhead; Dragging a key in its lane retimes it; Reset: modified marker, parameter and component reset, hold-to-compare, undoable revert; Sweep across a range and random variations apply on click and undo cleanly; Unticking a coordinate modifier bypasses it (identity), not zero; Only structure bypasses content; ticking one component includes its dependencies; Original restores; Insert a modifier on an input and replace a component while keeping its wiring; ✎ Edit: the text typesets live and previews on the canvas as a draft; Apply puts it in; an invalid equation is not applied; Cancel discards; Ticking components, walking stages and showing what one changes reuse one compiled program; GPU badge and performance dialog; Stage colors: signed colormap with range and histogram, coordinate grid, classic diagnostic on request; Profile plot of raw values along a line (V toggles it); Tabs: captioned steps; In & out (T is read by Add light as B); Ideas with plots; the tab stays across selections; Values view; dragging a parameter symbol changes it and undoes in one step; Equation Playground widens the panel, shows the step and its profile, keeps the pipeline as a strip; Escape backs out; Formula sheet: composition summary and per-component equations with bindings; ✎ Edit on a built-in: its steps as an equation, identical when applied; edits and param lines become sliders; Pop-out window shows the component and edits the scene; Alt-click raw probe; rulers readout with raw values; pin and unpin; Wheel zoom keeps the world point under the cursor fixed; Drag-and-drop onto a socket adds and wires a compatible component; incompatible drops are rejected; Drag wiring, drag-off disconnection, card checkboxes and the card eye button; Snapshot save and restore; Project JSON download and import round trip; PNG encoding + embedded UTF-8 project metadata; Four-frame deterministic PNG ZIP, CRCs, timestamps and visual change; Actual MediaRecorder video download; Exporting a stage uses the canvas colors (colormap, not the gray diagnostic); Context loss/recovery and float extension re-enablement | [Workflow report](WORKFLOW_VALIDATION.json) |
| GPU-unavailable fallback | Visible error panel, export rejected, no uncaught JS errors | [Failure report](FAILURE_VALIDATION.json) |
| Local server | Root page, JS MIME type and port-collision handling passed | [HTTP report](HTTP_VALIDATION.json) |

The unit tests cover all preset models, the component catalog (captioned steps for every component, help text and symbols for every parameter, symbols present in the typeset steps, finite key-function curves, known ideas, bypass sockets), graph types and traversal helpers, cycles including disconnected cycles, missing inputs and bypass semantics, reachable sharing, bounds, the program compiler (structural keys, parameter packing, run-time bypass and evaluation masks, view state, the compare pass), the equation language (parsing, type checking, error messages and suggestions, GLSL printing, parameters and definitions, library signatures, limits), Edit as equation, looks (colormaps, robust statistics, ranges, exposure gain, the CPU twin of the look pass), the formula sheet, GPU classification, TeX and equation typesetting with symbol roles, sweeps, seeded variations and original values, deletion, history, interpolation and key retiming, camera arithmetic and ruler ticks, graph layout, snapshots, deterministic frame times, ZIP CRC/path checks, and PNG metadata construction. The Python tests retain an independently written scalar source transcription as well as image/CLI/composition checks.

Browser workflow tests exercise actual event handlers, pointer gestures and downloads, not only direct function calls. PNG metadata was independently decoded with Pillow after loading all PNG chunks. Four-frame sequence ZIP output was independently checked using Python's zipfile module, including CRCs, frame dimensions, exact timestamps and differing frame pixels. A WebM was downloaded through MediaRecorder. The metadata-extraction utility also recovers the PNG's project without Pillow.

## Native GPU versus CPU measurement

The comparison uses the same source sampling grid, defaults and display function. No crop search, spatial alignment, fitted palette, blur or histogram correction was applied. Each error is measured across RGB **channel samples**, not a count of whole pixels.

| Metric | Result |
|---|---:|
| Mean absolute channel error, 0–255 | 0.293761 |
| Root mean squared channel error, 0–255 | 2.372531 |
| 95th percentile absolute channel error | 1 |
| 99th percentile absolute channel error | 4 |
| Largest single-channel error | **145** |
| RGB Pearson correlation | 0.999421624 |
| PSNR | 40.6266 dB |

**This is not byte-for-byte identity.** The relatively small average error coexists with large rare outliers near very sharp features. Correlation is a similarity statistic, not “percent accuracy.” The highest-frequency star/filament terms are sensitive to float32 coordinate and trigonometric rounding. The retained CPU image is itself a documented transcription of the supplied equations, not a recovered native original from the artist; agreement between implementations cannot prove the historical transcription is infallible.

The native draw plus readback took approximately **1.28 seconds** in the test environment (Chromium 153.0.8010.12 / ANGLE / SwiftShader software backend, in-memory about:blank harness; no URL navigation or policy modification). This number includes driver behavior and may include compilation; concurrent tests also affect it. It is **not a hardware benchmark** and should not be used to predict a user's frame rate.

## Raw field comparison

Readback used a one-pixel RGBA32F framebuffer at each selected source-grid position. These are actual field values before false-color diagnostic mapping, exposure or final display conversion. The seeded sample set, coordinates and complete statistics are in the JSON report.

| Field group | Mean absolute error | Maximum absolute error |
|---|---:|---:|
| shell | 5.88178867e-08 | 1.52253672e-06 |
| turbulence | 0.00160399817 | 0.00615996455 |
| cloud | 0.00503641878 | 0.0201153377 |
| gas | 0.000166188862 | 0.00105603023 |
| core | 7.15954347e-06 | 0.000274345955 |
| stars | 0.00275023095 | 0.0888712165 |
| final | 0.00281864616 | 0.0888772795 |

The source's fixed band-angle constants and powers are evaluated once in JavaScript float64 before being emitted as GLSL constants. An initial implementation that evaluated large fixed angles in shader precision had substantially worse raw-field disagreement. The delivered constant-folded version above is what the tests and screenshots use. High-frequency position-dependent calculations still use shader precision; constant folding cannot make those bitwise equivalent to float64.

The zero-strength lens-map identity was also verified through raw float readback. Offscreen probes and snapshots were checked to leave the visible canvas untouched. Changing a numeric parameter, bypassing a component and switching the canvas view were checked to reuse the same linked program, rather than forcing a compilation. Nonfinite tests check output RGBA, not merely the final displayed RGB.

## Browser verification and portability boundary

The tested browser was **Chromium 153.0.8010.12** with the **ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)** backend, reported highp precision of 23 bits, and `EXT_color_buffer_float` available. The automated suites select the ANGLE/SwiftShader software backend by default so that measurements are comparable across machines; the software backend still executes the actual shipped WebGL 2/GLSL code rather than a substitute Python renderer. Set `EQUATION_STUDIO_HARDWARE_GPU=1` to run them on the browser's own backend instead.

The tests loaded the **actual bundled HTML and JavaScript into an in-memory about:blank page**, so no browser policy is required and none was changed. Local file and localhost browser navigation were not end-to-end verified by these suites. The local static server was independently checked using ordinary loopback HTTP requests. The suites were also run on a hardware GPU during development (`EQUATION_STUDIO_HARDWARE_GPU=1`); those runs are not part of the figures above. Firefox, Safari and iOS remain untested.

Desktop (1600 × 1030, and 1920 × 1080 for the two-column playground), tablet (820 × 1180 with touch) and phone (390 × 844 with touch) screenshots were captured from the running app, with checks that the tablet and phone layouts do not scroll sideways and keep their bottom bar at the bottom of the screen. These test responsive layout in Chromium's device emulation; they are not device certification. Custom equation failure was tested to preserve the last valid project. Connection-cycle failure was tested to restore both the model and visible dropdown. GPU context loss and restoration were exercised using the test extension, including re-enabling float-buffer support and reading a field afterward.

Real-time recording produced a decodable video but does **not** guarantee a requested frame count on a software backend. The PNG sequence is the deterministic export path. Reference-image auto-alignment, a full accessibility audit, hardware performance matrix, memory-stress testing at all maximum limits, and browser codec matrix are not completed.

## Repeat the tests

Run from the project root:

```bash
node --test tests/*.test.js
python3 tools/build.py

# Optional Python reference dependencies and tests
python3 -m pip install -r reference/nebula_rewrite/requirements.txt
(cd reference/nebula_rewrite && python3 -m unittest discover -s tests -v)

# Optional browser-testing dependencies
python3 -m pip install playwright numpy pillow
python3 -m playwright install chromium     # or set EQUATION_STUDIO_CHROMIUM=/path/to/chromium
python3 tools/browser_check.py
python3 tools/workflow_check.py
python3 tools/gpu_validate.py
python3 tools/write_validation_report.py
```

On a headless Linux machine whose ANGLE driver needs X11, prefix a browser test with `xvfb-run -a`. Ordinary app launches do not force the software backend; the browser chooses its supported GPU backend. Do not alter managed browser policies to run these tests.

Full GPU validation can take several minutes on a software renderer. Run the browser suites sequentially when comparing performance or video capture behavior; concurrent software rendering competes for CPU resources. See [Development](DEVELOPMENT.md) for the complete regeneration checklist.

## What has not been verified

The five new subject studies do not have recovered original formula sheets or native reference images. No pixel-fidelity claim is made about those artworks. The Hedgehog/Fire video algorithms, third-party Rust implementation and unavailable thread/reply content were not reconstructed. The new astronomical/natural effects are not validated physical simulations. See the evidence ledger in [Research](RESEARCH.md).
